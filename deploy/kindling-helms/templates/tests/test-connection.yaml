apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kindling-helms.fullname" . }}-test-connection"
  labels:
    {{- include "kindling-helms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kindling-helms.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
