apiVersion: v1
kind: Service
metadata:
  name: {{ include "kindling-helms.fullname" . }}
  labels:
    {{- include "kindling-helms.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kindling-helms.selectorLabels" . | nindent 4 }}
