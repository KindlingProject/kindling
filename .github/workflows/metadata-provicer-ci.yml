name: Metadata-provider-CI

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-latest-test:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Set TAG
        run: echo "TAG=latesttest" >> $GITHUB_ENV
      - uses: actions/checkout@v3
      - name: Build and push
        uses: docker/build-push-action@v3
        with:
          context: ${{ github.workspace }}/collector/cmd/metadata-provider
          file: ${{ github.workspace }}/collector/Dockerfile-metadata-provider
          push: true
          tags: kindlingproject/metadata-provider:${{ env.TAG }}